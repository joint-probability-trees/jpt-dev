version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - JPT_NO_CYTHON=1 pip install ".[doc,matplotlib,plotly]"
    - sphinx-build -b html doc/source $READTHEDOCS_OUTPUT/html
